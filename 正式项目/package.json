{
  "description": "",
  "scripts": {
    "build:css": "cross-env NODE_ENV=production postcss src/tailwind.css -o dist/tailwind.css",
    "build": "postcss src/style.css -o dist/tailwind.css",
    "watch": "postcss build src/style.css -o dist/tailwind.css --watch"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.14",
    "cross-env": "^7.0.3",
    "postcss": "^8.4.24",
    "tailwindcss": "^3.3.2"
  }
}